Description: Verify that stored data is available via IPFS using calculated CID.
Network: {{ENV_PATH}}/polkadot_bulletin_local_network.toml
Creds: config

# Verify that the stored data "Hello World" is available via IPFS
alice: js-script ./verify-ipfs-data.js with '{ "data": "Hello World" }' within 60 seconds
